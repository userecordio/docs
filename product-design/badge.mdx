---
title: "Badge"
description: "Small inline label for status, counts, and categories. Always pill-shaped using the full-radius token."
icon: "tag"
---

## Variants

<Tabs>
  <Tab title="Default">
    <div style={{display:'flex',alignItems:'center',justifyContent:'center',padding:'40px 24px',background:'#f9fafb',borderRadius:'10px',border:'1px solid #e4e4e7',marginBottom:'16px'}}>
      <span style={{display:'inline-flex',alignItems:'center',justifyContent:'center',padding:'4px 10px',fontFamily:'DM Sans, sans-serif',fontSize:'12px',fontWeight:600,lineHeight:'16px',letterSpacing:'0.4px',borderRadius:'9999px',border:'1px solid transparent',background:'#18181b',color:'#fafafa',whiteSpace:'nowrap'}}>
        New
      </span>
    </div>

    General-purpose label for highlighting items. Uses `--base-primary` (zinc-900) background — highest visual weight of all variants.

    | Property | Token | Value |
    | --- | --- | --- |
    | Background | `--base-primary` | `#18181b` (zinc-900) |
    | Text | `--base-primary-foreground` | `#fafafa` (zinc-50) |
    | Border radius | `--border-radius-full` | `9999px` |
    | Font size | `--font-size-text-xs` | `12px` |
    | Font weight | `--font-weight-semibold` | `600` |
  </Tab>

  <Tab title="Secondary">
    <div style={{display:'flex',alignItems:'center',justifyContent:'center',padding:'40px 24px',background:'#f9fafb',borderRadius:'10px',border:'1px solid #e4e4e7',marginBottom:'16px'}}>
      <span style={{display:'inline-flex',alignItems:'center',justifyContent:'center',padding:'4px 10px',fontFamily:'DM Sans, sans-serif',fontSize:'12px',fontWeight:600,lineHeight:'16px',letterSpacing:'0.4px',borderRadius:'9999px',border:'1px solid transparent',background:'#f4f4f5',color:'#18181b',whiteSpace:'nowrap'}}>
        Draft
      </span>
    </div>

    Neutral, low-emphasis label. Uses `--base-secondary` (zinc-100) — sits quietly alongside primary content without drawing attention.

    | Property | Token | Value |
    | --- | --- | --- |
    | Background | `--base-secondary` | `#f4f4f5` (zinc-100) |
    | Text | `--base-secondary-foreground` | `#18181b` (zinc-900) |
    | Border radius | `--border-radius-full` | `9999px` |
  </Tab>

  <Tab title="Destructive">
    <div style={{display:'flex',alignItems:'center',justifyContent:'center',padding:'40px 24px',background:'#f9fafb',borderRadius:'10px',border:'1px solid #e4e4e7',marginBottom:'16px'}}>
      <span style={{display:'inline-flex',alignItems:'center',justifyContent:'center',padding:'4px 10px',fontFamily:'DM Sans, sans-serif',fontSize:'12px',fontWeight:600,lineHeight:'16px',letterSpacing:'0.4px',borderRadius:'9999px',border:'1px solid transparent',background:'#ee634e',color:'#fef2f2',whiteSpace:'nowrap'}}>
        Failed
      </span>
    </div>

    Error, danger, or removal state. Uses the brand `--base-destructive` orange token — reserved for critical status only.

    | Property | Token | Value |
    | --- | --- | --- |
    | Background | `--base-destructive` | `#ee634e` (brand orange) |
    | Text | `--base-destructive-foreground` | `#fef2f2` (red-50) |
    | Border radius | `--border-radius-full` | `9999px` |
  </Tab>

  <Tab title="Outline">
    <div style={{display:'flex',alignItems:'center',justifyContent:'center',padding:'40px 24px',background:'#f9fafb',borderRadius:'10px',border:'1px solid #e4e4e7',marginBottom:'16px'}}>
      <span style={{display:'inline-flex',alignItems:'center',justifyContent:'center',padding:'4px 10px',fontFamily:'DM Sans, sans-serif',fontSize:'12px',fontWeight:600,lineHeight:'16px',letterSpacing:'0.4px',borderRadius:'9999px',border:'1px solid #e4e4e7',background:'transparent',color:'#18181b',whiteSpace:'nowrap'}}>
        Design
      </span>
    </div>

    Minimal, bordered badge with no fill. Ideal for category tags and filter chips where color should not imply status.

    | Property | Token | Value |
    | --- | --- | --- |
    | Background | transparent | — |
    | Text | `--base-foreground` | `#18181b` (zinc-900) |
    | Border | `--base-border` | `#e4e4e7` (zinc-200) |
    | Border radius | `--border-radius-full` | `9999px` |
  </Tab>

  <Tab title="Success">
    <div style={{display:'flex',alignItems:'center',justifyContent:'center',padding:'40px 24px',background:'#f9fafb',borderRadius:'10px',border:'1px solid #e4e4e7',marginBottom:'16px'}}>
      <span style={{display:'inline-flex',alignItems:'center',justifyContent:'center',padding:'4px 10px',fontFamily:'DM Sans, sans-serif',fontSize:'12px',fontWeight:600,lineHeight:'16px',letterSpacing:'0.4px',borderRadius:'9999px',border:'1px solid transparent',background:'#dcfce7',color:'#166534',whiteSpace:'nowrap'}}>
        Active
      </span>
    </div>

    Positive status indicator. Uses the Tailwind green palette directly — green-100 background with green-800 text for accessible contrast.

    | Property | Token | Value |
    | --- | --- | --- |
    | Background | `--tailwind-colors-green-100` | `#dcfce7` |
    | Text | `--tailwind-colors-green-800` | `#166534` |
    | Border radius | `--border-radius-full` | `9999px` |
  </Tab>

  <Tab title="Warning">
    <div style={{display:'flex',alignItems:'center',justifyContent:'center',padding:'40px 24px',background:'#f9fafb',borderRadius:'10px',border:'1px solid #e4e4e7',marginBottom:'16px'}}>
      <span style={{display:'inline-flex',alignItems:'center',justifyContent:'center',padding:'4px 10px',fontFamily:'DM Sans, sans-serif',fontSize:'12px',fontWeight:600,lineHeight:'16px',letterSpacing:'0.4px',borderRadius:'9999px',border:'1px solid transparent',background:'#fef3c7',color:'#92400e',whiteSpace:'nowrap'}}>
        Pending
      </span>
    </div>

    Caution or in-progress state. Uses the Tailwind amber palette — amber-100 background with amber-800 text for clear visibility without alarming urgency.

    | Property | Token | Value |
    | --- | --- | --- |
    | Background | `--tailwind-colors-amber-100` | `#fef3c7` |
    | Text | `--tailwind-colors-amber-800` | `#92400e` |
    | Border radius | `--border-radius-full` | `9999px` |
  </Tab>
</Tabs>

---

## Design Tokens Used

| Property | Token | Value |
|----------|-------|-------|
| Border radius | `--border-radius-full` | `9999px` |
| Padding | `--spacing-1` `--spacing-2-5` | `4px 10px` |
| Font size | `--font-size-text-xs` | `12px` |
| Font weight | `--font-weight-semibold` | `600` |
| Line height | `--font-line-height-leading-4` | `16px` |
| Background (default) | `--base-primary` | zinc-900 / zinc-50 |
| Text (default) | `--base-primary-foreground` | zinc-50 / zinc-900 |
| Background (secondary) | `--base-secondary` | zinc-100 / zinc-800 |
| Text (secondary) | `--base-secondary-foreground` | zinc-900 / zinc-50 |
| Background (destructive) | `--base-destructive` | `#ee634e` |
| Border (outline) | `--base-border` | zinc-200 / zinc-800 |

---

## CSS Implementation

```css
.badge {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--spacing-1);
  padding: var(--spacing-1) var(--spacing-2-5);
  font-family: var(--typography-font-family-font-sans);
  font-size: var(--font-size-text-xs);
  font-weight: var(--font-weight-semibold);
  line-height: var(--font-line-height-leading-4);
  letter-spacing: var(--font-letter-spacing-wide);
  border-radius: var(--border-radius-full);
  border: var(--border-width-border-1) solid transparent;
  white-space: nowrap;
}

/* Variants */
.badge--default {
  background-color: var(--base-primary);
  color: var(--base-primary-foreground);
}

.badge--secondary {
  background-color: var(--base-secondary);
  color: var(--base-secondary-foreground);
}

.badge--destructive {
  background-color: var(--base-destructive);
  color: var(--base-destructive-foreground);
}

.badge--outline {
  background-color: transparent;
  color: var(--base-foreground);
  border-color: var(--base-border);
}

.badge--success {
  background-color: var(--tailwind-colors-green-100);
  color: var(--tailwind-colors-green-800);
}

.badge--warning {
  background-color: var(--tailwind-colors-amber-100);
  color: var(--tailwind-colors-amber-800);
}
```

---

## React / TSX

```tsx
import { cn } from "@/lib/utils";
import { HTMLAttributes } from "react";

type BadgeVariant = "default" | "secondary" | "destructive" | "outline" | "success" | "warning";

interface BadgeProps extends HTMLAttributes<HTMLSpanElement> {
  variant?: BadgeVariant;
}

const variantClass: Record<BadgeVariant, string> = {
  default: "badge--default",
  secondary: "badge--secondary",
  destructive: "badge--destructive",
  outline: "badge--outline",
  success: "badge--success",
  warning: "badge--warning",
};

export function Badge({ variant = "default", className, ...props }: BadgeProps) {
  return (
    <span
      className={cn("badge", variantClass[variant], className)}
      {...props}
    />
  );
}
```

---

## Usage Examples

```tsx
{/* Status badges */}
<Badge variant="success">Active</Badge>
<Badge variant="warning">Pending</Badge>
<Badge variant="destructive">Failed</Badge>
<Badge variant="secondary">Draft</Badge>

{/* Category tag */}
<Badge variant="outline">Design</Badge>

{/* Count badge */}
<Badge>12 new</Badge>

{/* With icon */}
<Badge variant="success">
  <CheckIcon size={10} />
  Verified
</Badge>
```

---

## Accessibility

<Note>
  Badges are purely visual. If a badge conveys important status information, also communicate it to screen readers via `aria-label` on the parent element or `<span className="sr-only">` inside the badge.
</Note>

```tsx
<button aria-label="Notifications, 3 unread">
  <BellIcon />
  <Badge aria-hidden="true">3</Badge>
</button>
```
