---
title: "Input"
description: "Text input field with label, helper text, and error state support. Built on semantic color and spacing tokens."
icon: "input-text"
---

## States

<Tabs>
  <Tab title="Default">
    <div style={{display:'flex',alignItems:'center',justifyContent:'center',padding:'40px 24px',background:'#f9fafb',borderRadius:'10px',border:'1px solid #e4e4e7',marginBottom:'16px'}}>
      <div style={{display:'flex',flexDirection:'column',gap:'6px',width:'280px'}}>
        <label style={{fontFamily:'DM Sans, sans-serif',fontSize:'14px',fontWeight:500,color:'#18181b'}}>Email address</label>
        <input readOnly defaultValue="" placeholder="you@example.com" style={{width:'100%',height:'40px',padding:'0 12px',fontFamily:'DM Sans, sans-serif',fontSize:'14px',color:'#18181b',background:'#ffffff',border:'1px solid #e4e4e7',borderRadius:'6px',outline:'none',boxSizing:'border-box'}} />
        <span style={{fontFamily:'DM Sans, sans-serif',fontSize:'12px',color:'#71717a'}}>We'll never share your email.</span>
      </div>
    </div>

    The resting state of the input. Border uses `--base-input` (zinc-200), background is `--base-background` (white).

    | Property | Token | Value |
    | --- | --- | --- |
    | Border | `--base-input` | `#e4e4e7` (zinc-200) |
    | Background | `--base-background` | `#ffffff` |
    | Text | `--base-foreground` | `#18181b` (zinc-900) |
    | Border radius | `--border-radius-md` | `6px` |
  </Tab>

  <Tab title="Focus">
    <div style={{display:'flex',alignItems:'center',justifyContent:'center',padding:'40px 24px',background:'#f9fafb',borderRadius:'10px',border:'1px solid #e4e4e7',marginBottom:'16px'}}>
      <div style={{display:'flex',flexDirection:'column',gap:'6px',width:'280px'}}>
        <label style={{fontFamily:'DM Sans, sans-serif',fontSize:'14px',fontWeight:500,color:'#18181b'}}>Email address</label>
        <input readOnly defaultValue="you@example.com" style={{width:'100%',height:'40px',padding:'0 12px',fontFamily:'DM Sans, sans-serif',fontSize:'14px',color:'#18181b',background:'#ffffff',border:'1px solid #18181b',borderRadius:'6px',outline:'none',boxShadow:'0 0 0 2px #18181b',boxSizing:'border-box'}} />
        <span style={{fontFamily:'DM Sans, sans-serif',fontSize:'12px',color:'#71717a'}}>We'll never share your email.</span>
      </div>
    </div>

    Active/focused state. Border and focus ring both use `--base-ring` (zinc-900), applied via `box-shadow`.

    | Property | Token | Value |
    | --- | --- | --- |
    | Border | `--base-ring` | `#18181b` (zinc-900) |
    | Focus ring | `--base-ring` | `box-shadow: 0 0 0 2px #18181b` |
    | Background | `--base-background` | `#ffffff` |
    | Text | `--base-foreground` | `#18181b` (zinc-900) |
  </Tab>

  <Tab title="Disabled">
    <div style={{display:'flex',alignItems:'center',justifyContent:'center',padding:'40px 24px',background:'#f9fafb',borderRadius:'10px',border:'1px solid #e4e4e7',marginBottom:'16px'}}>
      <div style={{display:'flex',flexDirection:'column',gap:'6px',width:'280px'}}>
        <label style={{fontFamily:'DM Sans, sans-serif',fontSize:'14px',fontWeight:500,color:'#71717a'}}>API key</label>
        <input readOnly disabled placeholder="sk-••••••••" style={{width:'100%',height:'40px',padding:'0 12px',fontFamily:'DM Sans, sans-serif',fontSize:'14px',color:'#71717a',background:'#f4f4f5',border:'1px solid #e4e4e7',borderRadius:'6px',outline:'none',opacity:0.5,cursor:'not-allowed',boxSizing:'border-box'}} />
        <span style={{fontFamily:'DM Sans, sans-serif',fontSize:'12px',color:'#71717a'}}>Contact support to update your key.</span>
      </div>
    </div>

    Non-interactive state. Background shifts to `--base-muted`, text and border use muted tokens, cursor is `not-allowed`.

    | Property | Token | Value |
    | --- | --- | --- |
    | Background | `--base-muted` | `#f4f4f5` (zinc-100) |
    | Border | `--base-border` | `#e4e4e7` (zinc-200) |
    | Text | `--base-muted-foreground` | `#71717a` (zinc-500) |
    | Opacity | — | `0.5` |
    | Cursor | — | `not-allowed` |
  </Tab>

  <Tab title="Error">
    <div style={{display:'flex',alignItems:'center',justifyContent:'center',padding:'40px 24px',background:'#f9fafb',borderRadius:'10px',border:'1px solid #e4e4e7',marginBottom:'16px'}}>
      <div style={{display:'flex',flexDirection:'column',gap:'6px',width:'280px'}}>
        <label style={{fontFamily:'DM Sans, sans-serif',fontSize:'14px',fontWeight:500,color:'#18181b'}}>Username</label>
        <input readOnly defaultValue="johndoe" style={{width:'100%',height:'40px',padding:'0 12px',fontFamily:'DM Sans, sans-serif',fontSize:'14px',color:'#18181b',background:'#ffffff',border:'1px solid #ee634e',borderRadius:'6px',outline:'none',boxSizing:'border-box'}} />
        <span style={{fontFamily:'DM Sans, sans-serif',fontSize:'12px',color:'#ee634e'}}>Username is already taken.</span>
      </div>
    </div>

    Validation failure state. Border and error message both use `--base-destructive` (brand orange). Also applies a destructive focus ring when active.

    | Property | Token | Value |
    | --- | --- | --- |
    | Border | `--base-destructive` | `#ee634e` (brand orange) |
    | Error text | `--base-destructive` | `#ee634e` |
    | Background | `--base-background` | `#ffffff` |
    | Text | `--base-foreground` | `#18181b` (zinc-900) |
  </Tab>

  <Tab title="Placeholder">
    <div style={{display:'flex',alignItems:'center',justifyContent:'center',padding:'40px 24px',background:'#f9fafb',borderRadius:'10px',border:'1px solid #e4e4e7',marginBottom:'16px'}}>
      <div style={{display:'flex',flexDirection:'column',gap:'6px',width:'280px'}}>
        <label style={{fontFamily:'DM Sans, sans-serif',fontSize:'14px',fontWeight:500,color:'#18181b'}}>Search</label>
        <div style={{position:'relative',width:'100%'}}>
          <input readOnly defaultValue="" style={{width:'100%',height:'40px',padding:'0 12px',fontFamily:'DM Sans, sans-serif',fontSize:'14px',color:'#71717a',background:'#ffffff',border:'1px solid #e4e4e7',borderRadius:'6px',outline:'none',boxSizing:'border-box'}} />
          <span style={{position:'absolute',left:'12px',top:'50%',transform:'translateY(-50%)',fontFamily:'DM Sans, sans-serif',fontSize:'14px',color:'#71717a',pointerEvents:'none'}}>Search anything...</span>
        </div>
      </div>
    </div>

    Placeholder text appears when the field is empty. Uses `--base-muted-foreground` (zinc-500) — intentionally lighter than typed text to distinguish hint from value.

    | Property | Token | Value |
    | --- | --- | --- |
    | Placeholder color | `--base-muted-foreground` | `#71717a` (zinc-500) |
    | Border | `--base-input` | `#e4e4e7` (zinc-200) |
    | Background | `--base-background` | `#ffffff` |
  </Tab>
</Tabs>

---

## Design Tokens Used

| Property | Token | Value |
|----------|-------|-------|
| Background | `--base-background` | white / zinc-950 |
| Border (default) | `--base-input` | zinc-200 / zinc-800 |
| Border (focus) | `--base-ring` | zinc-900 / zinc-300 |
| Border (error) | `--base-destructive` | `#ee634e` |
| Text | `--base-foreground` | zinc-900 / zinc-50 |
| Placeholder | `--base-muted-foreground` | zinc-500 / zinc-400 |
| Border radius | `--border-radius-md` | `6px` |
| Height | `--height-h-10` | `40px` |
| Padding (horizontal) | `--spacing-3` | `12px` |
| Font size | `--font-size-text-sm` | `14px` |
| Label font weight | `--font-weight-medium` | `500` |

---

## CSS Implementation

```css
.input-wrapper {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-1-5);
}

.input-label {
  font-family: var(--typography-font-family-font-sans);
  font-size: var(--font-size-text-sm);
  font-weight: var(--font-weight-medium);
  color: var(--base-foreground);
  line-height: var(--font-line-height-leading-5);
}

.input {
  width: 100%;
  height: var(--height-h-10);
  padding: 0 var(--spacing-3);
  font-family: var(--typography-font-family-font-sans);
  font-size: var(--font-size-text-sm);
  color: var(--base-foreground);
  background-color: var(--base-background);
  border: var(--border-width-border-1) solid var(--base-input);
  border-radius: var(--border-radius-md);
  outline: none;
  transition: border-color 0.15s, box-shadow 0.15s;
}

.input::placeholder {
  color: var(--base-muted-foreground);
}

.input:focus {
  border-color: var(--base-ring);
  box-shadow: 0 0 0 2px var(--base-ring);
}

.input:disabled {
  background-color: var(--base-muted);
  border-color: var(--base-border);
  color: var(--base-muted-foreground);
  cursor: not-allowed;
  opacity: 0.5;
}

/* Error state */
.input--error {
  border-color: var(--base-destructive);
}

.input--error:focus {
  border-color: var(--base-destructive);
  box-shadow: 0 0 0 2px var(--base-destructive);
}

.input-helper {
  font-size: var(--font-size-text-xs);
  color: var(--base-muted-foreground);
  line-height: var(--font-line-height-leading-4);
}

.input-helper--error {
  color: var(--base-destructive);
}
```

---

## React / TSX

```tsx
import { cn } from "@/lib/utils";
import { InputHTMLAttributes, forwardRef } from "react";

interface InputProps extends InputHTMLAttributes<HTMLInputElement> {
  label?: string;
  helper?: string;
  error?: string;
}

export const Input = forwardRef<HTMLInputElement, InputProps>(
  ({ label, helper, error, className, id, ...props }, ref) => {
    const inputId = id ?? label?.toLowerCase().replace(/\s+/g, "-");

    return (
      <div className="input-wrapper">
        {label && (
          <label htmlFor={inputId} className="input-label">
            {label}
          </label>
        )}
        <input
          ref={ref}
          id={inputId}
          className={cn("input", error && "input--error", className)}
          aria-invalid={!!error}
          aria-describedby={error ? `${inputId}-error` : helper ? `${inputId}-helper` : undefined}
          {...props}
        />
        {error ? (
          <p id={`${inputId}-error`} className="input-helper input-helper--error">
            {error}
          </p>
        ) : helper ? (
          <p id={`${inputId}-helper`} className="input-helper">
            {helper}
          </p>
        ) : null}
      </div>
    );
  }
);

Input.displayName = "Input";
```

---

## Usage Example

```tsx
<Input
  label="Email address"
  type="email"
  placeholder="you@example.com"
  helper="We'll never share your email."
/>

<Input
  label="Username"
  defaultValue="johndoe"
  error="Username is already taken."
/>

<Input
  label="API key"
  type="password"
  disabled
  placeholder="sk-••••••••"
/>
```

<Note>
  Always pair an `<Input>` with a visible `label`. For icon-only search inputs, provide an `aria-label` instead.
</Note>
